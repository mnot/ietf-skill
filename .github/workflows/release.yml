name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  attach-zips:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: make zip
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*.zip
